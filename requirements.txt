# Ego Language-Annotation Atlas: analysis stack (versions verified 2026-06-28,
# Python 3.13, Windows). Downloading uses only the standard library + requests.
# Clustering uses scikit-learn's built-in HDBSCAN (no separate hdbscan build).

# data + io
numpy==2.4.6
pandas==3.0.4
pyarrow==24.0.0
requests==2.32.5

# embedding + clustering
sentence-transformers==5.6.0
scikit-learn==1.9.0
scipy==1.18.0
umap-learn==0.5.12

# pulled in transitively by sentence-transformers / umap-learn, pinned for repro
torch==2.12.1
transformers==5.12.1
huggingface-hub==1.21.0
numba==0.65.1
llvmlite==0.47.0
pynndescent==0.6.0
